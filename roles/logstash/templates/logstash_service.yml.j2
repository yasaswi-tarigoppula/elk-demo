apiVersion: v1
kind: Service
metadata:
  name: logstash
  labels:
    component: logstash
spec:
  type: NodePort
  selector:
    component: logstash
  ports:
    - name: logstash
      port: 5044
      nodePort: {{ logstash_nodeport }}

